[project]
name = "agentstate-guard"
version = "0.9.0.dev0"
description = "AgentState Guard — protect Claude Code / CloudCLI / Docker / Tailscale environment state"
requires-python = ">=3.11"
dependencies = [
    "cryptography>=41.0",
    "fastapi>=0.115",
    "uvicorn>=0.34",
    "pydantic>=2.0",
]

[project.optional-dependencies]
yaml = ["pyyaml"]
dev = ["pytest", "ruff", "mypy"]

[project.scripts]
agentguard = "agentguard.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["agentguard*"]

[tool.pytest.ini_options]
addopts = "--ignore=tests/test_device_link_audit.py --ignore=tests/test_device_link_e2e.py --ignore=tests/test_device_link_integration.py"

[tool.setuptools.package-data]
agentguard = ["web_static/**/*"]
